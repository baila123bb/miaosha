<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="miaosha.dao.MemberDao">
	<select id="getMemberById" parameterType="int" resultType="miaosha.model.Member">
		select * from t_mem_member where id=#{id}
	</select>
	

</mapper>